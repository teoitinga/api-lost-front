import { ListarCliente.Model } from './listar-cliente.model';

describe('ListarCliente.Model', () => {
  it('should create an instance', () => {
    expect(new ListarCliente.Model()).toBeTruthy();
  });
});
