import { CarregadoDirective } from './carregado.directive';

describe('CarregadoDirective', () => {
  it('should create an instance', () => {
    const directive = new CarregadoDirective();
    expect(directive).toBeTruthy();
  });
});
