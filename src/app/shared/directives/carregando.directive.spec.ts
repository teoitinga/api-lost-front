import { CarregandoDirective } from './carregando.directive';

describe('CarregandoDirective', () => {
  it('should create an instance', () => {
    const directive = new CarregandoDirective();
    expect(directive).toBeTruthy();
  });
});
